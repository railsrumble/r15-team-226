<%= form_for(@adoption_response) do |f| %>
  <% if @adoption_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adoption_response.errors.count, "error") %> prohibited this adoption_response from being saved:</h2>

      <ul>
      <% @adoption_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :adoption_request_id %><br>
    <%= f.number_field :adoption_request_id %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.number_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
